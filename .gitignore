# See http://help.github.com/ignore-files/ for more about ignoring files.

# dependencies
node_modules

# testing
coverage

# production
build

# misc
.DS_Store
.env
npm-debug.log

/bets.sqlite

# It should be committed, but unfortunately 'npm install' changes the order on each install which breaks automated deploys.
# Might be allowed later when NPM becomes more stable.
/package-lock.json